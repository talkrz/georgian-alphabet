import alphabetDefinition from './../../alphabetDefinition';

const initialState = {
  alphabetDefinition,
};

const alphabet = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default alphabet;
